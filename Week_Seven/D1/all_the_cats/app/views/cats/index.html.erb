<h1>ALL THE CATS. ALL. OF. THEM.</h1>
<ul>
    <% @cats.each do |cat| %>
        <li><%= cat.name %>, (<%= cat.sex %>)</li>
        <a href="<%= cat_url(cat) %>%">CHECK OUT THIS CAT!</a>
    <% end %>
</ul>
